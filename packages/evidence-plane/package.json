{
  "name": "@security-lab/evidence-plane",
  "version": "0.1.0",
  "description": "Evidence and audit plane: campaign evidence store, hashed event stream, run manifests, and report rendering.",
  "private": true,
  "license": "Apache-2.0",
  "author": "Chris Walker",
  "repository": {
    "type": "git",
    "url": "https://github.com/blockbrain-ai/security-lab.git"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --test --import tsx $(find src -name '*.test.ts' | sort)",
    "test:coverage": "node --test --experimental-test-coverage --import tsx $(find src -name '*.test.ts' | sort)",
    "evidence:report": "tsx src/cli/report.ts"
  }
}
